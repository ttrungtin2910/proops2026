# Agent-OS Standards Index
# Auto-generated by /discover-standards. Do not edit manually.
#
# Format:
#   folder/filename (without .md):
#     description: one-line description for matching

# Root standards (no subfolder)
root:
  # example: add your cross-cutting standards here
  # ai-first-approach:
  #   description: How to approach any DevOps task using Claude Code first

# Docker standards
docker:
  # dockerfile-pattern:
  #   description: Multi-stage Dockerfile pattern for production images

# Kubernetes standards
kubernetes:
  # manifest-structure:
  #   description: Required fields in every K8s Deployment manifest

# CI/CD standards
cicd:
  # pipeline-structure:
  #   description: Standard stages and gates for CI/CD pipelines

# Infrastructure standards
iac:
  # terraform-module-pattern:
  #   description: Module structure and naming for Terraform

# Observability standards
observability:
  # alert-design:
  #   description: Principles for writing actionable alerts

# Security standards
security:
  # secret-management:
  #   description: How to handle secrets in K8s, CI/CD, and IaC
