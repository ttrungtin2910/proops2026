# Memory Gap Analysis

| File / Concept | Why weak | Specific fix | Target |
|----------------|----------|--------------|--------|
| **PHASE 1 — AUDIT GAPS (0/2 and 1/2)** | | | |
| `networking-basics.md` ❌ 0/2 | Covers L3–L4 only (DNS/ports/TLS/traceroute). Zero content on Docker bridge networks, K8s CoreDNS, or service-name resolution. `kafka:9092`, `milvus:19530`, `redis:6379` never appear. | Add §6 Container Networking: Docker bridge DNS, `docker network inspect`, Compose service-name resolution, K8s CoreDNS `<svc>.<ns>.svc.cluster.local`, real RAG stack hostnames (kafka/milvus/redis/hook-gateway) | **Day 21** |
| `aws.md` ❌ 0/2 | Every resource ID is a placeholder (`vpc-0abc123`, `sg-0abc123`). Real VPC, subnets, SGs, S3 buckets from Day 06 live only in `aws-lab-day06.md`. No cross-reference. | Add §6 Day 06 Lab Overlay: real VPC `vpc-09c073830f75e7a64`, subnet IDs, SG `sg-07b030604bb741cf4`, S3 buckets `tin-tt-public-assets`/`tin-tt-private-assets`, real IAM error (tag enforcement on `ec2:CreateKeyPair`), S3 self-lockout gotcha | **Day 21** |
| `software-architecture.md` ⚠️ 1/2 | Generic patterns only. No mention of hook-gateway/qna-agent/websocket-responder, no data model (RAG+Kafka+Milvus+MongoDB), no Kafka topic flow, no rollback strategy for this specific stack. | Add §6 Mock Project Architecture: service map, Kafka topic flow (`dev.qna.incoming.msg` → `dev.response.msg`), data model (JWT→Redis, vectors→Milvus, history→MongoDB, events→Kafka), rollback strategy | **Day 22** |
| `linux-basics.md` ⚠️ 1/2 | All 5 sections present but every command is generic reference. No cross-reference to `eksctl`, `kubectl`, `aws ecr`, `docker exec` used in actual sessions (Day 11/16). | Add real command examples per section: Day 11 `docker exec -it` debug, Day 16 `eksctl create cluster`, Day 16 `aws ecr get-login-password \| docker login` | **Day 23** |
| `cloud-concepts.md` ⚠️ 1/2 | All AZ/region examples use `us-east-1`. Actual region (`ap-northeast-2` Seoul) and Day 11/19 EC2 sizing decisions (`t3.micro` free tier + `t3.medium SPOT` for EKS) never documented. | Add §6 My Region Choices: why Seoul (`ap-northeast-2`), Day 11 `t3.micro` free-tier decision, Day 19 `t3.medium SPOT` node group choice | **Day 23** |
| `kubernetes-core.md` ⚠️ 1/2 | CrashLoopBackOff row in diagnostic table is generic — no reference to actual `kafka` or `qna-agent` crashes from Day 12/14. | Add cross-reference footnote to `kubernetes-project.md` Failures rows 1+3+4: kafka `enableServiceLinks: false` fix, qna-agent MilvusClient deadlock | **Day 22** |
| `kubernetes-advanced.md` ⚠️ 1/2 | HPA section describes YAML and mechanics. No real scale event observed and logged. `kubectl get hpa -w` output with replica change never recorded. | Run HPA lab with `kubectl run load-gen --image=busybox -- /bin/sh -c "while true; do wget -q -O- http://web-svc; done"`, record `kubectl get hpa -w` output showing replica change (e.g., 2→4) with timestamp | **Day 28** |
| `iac-basics.md` ⚠️ 1/2 | Commands listed but actual `terraform plan` output (`Plan: 1 to add, 0 to change, 0 to destroy`) and `Apply complete! Resources: 1 added` text not preserved. | Paste real `Plan:` and `Apply complete!` lines from Day 19 session (check `daily-reports/day-19-report.md`) | **Day 21** |
| `docker.md` §19 Secrets | Lists `cp` commands only. No per-service env var table (hook-gateway/qna-agent env vars), no "forgot .env.example" lesson entry. | Add env var table for hook-gateway/qna-agent + lesson: always commit `.env.example` (not `.env`) to avoid "service starts but env is empty" silent failure | **Day 21** |
| `docker.md` §10 Observability | Placeholder labels and fake log group path, no RAM baselines for real services. | Replace with `docker stats` baselines per service + Milvus OOM lesson (requires >4GB RAM limit in Compose, exits silently otherwise) | **Day 22** |
| **PHASE 2 — QUIZ -1 PENALTIES (wrong-but-confident)** | | | |
| `docker.md` — `compose down -v` | Said `docker compose down` removes named volumes. **WRONG.** `down` stops + removes containers; volumes are **kept**. Only `down -v` deletes named volumes. Stated confidently, no hedge. | Add prominent callout to docker.md §14: `down` → containers removed, **volumes KEPT** / `down -v` → volumes deleted. Critical for production: `down` without `-v` will NOT wipe the database. | **Day 21** |
| `kubernetes-advanced.md` — Secret encoding | Q14: answered about CPU resource requests (wrong question). Real question: confirm base64 ≠ encryption. Stated a wrong answer confidently about a completely different topic. | Anchor: `echo "c3VwZXJzZWNyZXQ=" \| base64 -d` = "supersecret" — no key required. Etcd encryption-at-rest is off by default, must be explicitly enabled by cluster admin. Already in the CORRECTION box — review before any exam. | **Day 21 (review)** |
| `kubernetes-advanced.md` — HPA diagnostic | Q15: said `kubectl get deployment -o yaml` for HPA `<unknown>` targets. **WRONG.** Deployment YAML shows spec only — not whether metrics-server is collecting data. | Fix: `kubectl top pods` → if `error: Metrics API not available` → metrics-server not running. `kubectl get deployment -o yaml` is useless for this diagnosis. Already in diagnostic table — anchor it. | **Day 21 (review)** |
| **PHASE 3 — QUIZ 0-SCORE PARTIALS** | | | |
| `docker.md` — exit code 137 | Said exit code 138. Correct: 137 = 128 + SIGKILL (signal 9). Stated with apparent confidence. | Memorize: 128 + signal number = exit code. SIGKILL=9 → **137**. SIGTERM=15 → 143. OOM (kernel) = 137. Add to docker.md §16 exit codes section. | **Day 21** |
| `kubernetes-core.md` — apply vs create | Mechanics correct (apply = create-or-update, create = fails if exists). Missing: "when does it matter" = CI/CD pipelines where `create` fails on 2nd run, `apply` succeeds idempotently. | Add one-line note to apply vs create section: "In CI/CD, always use `apply` — `create` fails with `AlreadyExists` on every pipeline re-run after the first." | **Day 22** |
| `eks-practice.md` — orphan resources | Said Security Groups instead of VPC. Correct: EBS volumes, Load Balancers, **VPC** (SGs live inside VPC — deleted when VPC is deleted). | Add correction note to eks-practice.md §9: three orphan checks = EBS volumes + Load Balancers + **VPC** (not SGs — SGs die with VPC). | **Day 22** |
| `bash-scripting.md` — `--dry-run` implementation | Gave WHY correctly but missed the HOW: the `run()` wrapper from `scripts/deploy.sh` lines 27–33 that prints commands in dry-run mode instead of executing them. | Add §5.1 run() wrapper pattern to bash-scripting.md: `if $DRY_RUN; then echo "[DRY-RUN] $*"; else "$@"; fi`. Reference deploy.sh lines 27–33 as the real implementation. | **Day 22** |
| `iac-basics.md` — .gitignore reasons | Three files listed correctly (`.terraform/`, `*.tfstate`, `*.tfvars`). WHY each must not be committed was completely missing. | Add "why" to each: `.terraform/` = 100MB provider binaries (bloats repo, OS-specific); `*.tfstate` = resource IDs + secrets in plaintext; `*.tfvars` = actual credential values. Commit any of these = security incident. | **Day 21** |
